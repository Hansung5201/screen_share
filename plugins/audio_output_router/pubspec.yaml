name: audio_output_router
version: 0.0.1
description: Stub plugin for selecting Windows audio output.
environment:
  sdk: '>=3.0.0 <4.0.0'
flutter:
  plugin:
    platforms:
      windows:
        pluginClass: AudioOutputRouterPlugin
